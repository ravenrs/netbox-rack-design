# See PEP 518 for the spec of this file
# https://www.python.org/dev/peps/pep-0518/

[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name =  "netbox-rack-design"
version = "0.26.0"
authors = [
    {name = "Petr Voronov", email = "ravenrs@gmail.com"},
]
description = "Plan rack changes as versioned designs — a visual multi-rack editor with naming and power projection, read-only over your live DCIM data until you apply."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = ["netbox", "netbox-plugin"]

classifiers=[
    'Development Status :: 4 - Beta',
    'Framework :: Django',
    # The supported NetBox range spans three Django majors: 4.4.x and 4.5.x ship
    # Django 5.2, 4.6.x ships Django 6.0.
    'Framework :: Django :: 5.2',
    'Framework :: Django :: 6.0',
    'Intended Audience :: Developers',
    'Intended Audience :: System Administrators',
    'Intended Audience :: Telecommunications Industry',
    'Natural Language :: English',
    'Programming Language :: Python :: 3 :: Only',
    'Programming Language :: Python :: 3.12',
    'Programming Language :: Python :: 3.13',
    'Programming Language :: Python :: 3.14',
    'Topic :: System :: Networking',
]

requires-python = ">=3.12.0"

[project.optional-dependencies]
test = [
    "check-manifest==0.51",
    "ruff==0.14.14",
    "pre-commit==4.6.0",
    "pytest==9.0.2",
]

[project.urls]
Documentation = "https://ravenrs.github.io/netbox-rack-design/"
Source = "https://github.com/ravenrs/netbox-rack-design"
Tracker = "https://github.com/ravenrs/netbox-rack-design/issues"
Tutorial = "https://www.youtube.com/playlist?list=PLQrzYAZqdcXg"

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501", # line too long, handled by ruff-format
    "B008", # do not perform function calls in argument defaults
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.setuptools.packages.find]
where = ["."]
include = ["netbox_rack_design*"]

[tool.setuptools.package-data]
netbox_rack_design = [
    "templates/**",
    "static/**",
    "locale/**/*.mo",
]
